import { Direction, Directionality } from '@angular/cdk/bidi';
import { NumberInput } from '@angular/cdk/coercion';
import { ChangeDetectorRef, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { NgStyleInterface } from 'tds-ui/core/config';
import { TDSProgressCirclePath, TDSProgressFormatter, TDSProgressGapPositionType, TDSProgressStatusType, TDSProgressStepItem, TDSProgressStrokeColorType, TDSProgressStrokeLinecapType, TDSProgressTypeType } from './typings';
import * as i0 from "@angular/core";
export declare class TDSProgressComponent implements OnChanges, OnInit, OnDestroy {
    private cdr;
    private directionality;
    static ngAcceptInputType_successPercent: NumberInput;
    static ngAcceptInputType_percent: NumberInput;
    static ngAcceptInputType_ptrokeWidth: NumberInput;
    static ngAcceptInputType_gapDegree: NumberInput;
    static ngAcceptInputType_steps: NumberInput;
    showInfo: boolean;
    width: number;
    strokeColor?: TDSProgressStrokeColorType;
    size: 'default' | 'small';
    format?: TDSProgressFormatter;
    successPercent?: number;
    percent: number;
    strokeWidth?: number;
    gapDegree?: number;
    status?: TDSProgressStatusType;
    type: TDSProgressTypeType;
    gapPosition: TDSProgressGapPositionType;
    strokeLinecap: TDSProgressStrokeLinecapType;
    steps: number;
    _steps: TDSProgressStepItem[];
    /** Gradient style when `type` is `line`. */
    lineGradient: string | null;
    /** If user uses gradient color. */
    isGradient: boolean;
    /** If the linear progress is a step progress. */
    isSteps: boolean;
    /**
     * Each progress whose `type` is circle or dashboard should have unique id to
     * define `<linearGradient>`.
     */
    gradientId: number;
    /** Paths to rendered in the template. */
    progressCirclePath: TDSProgressCirclePath[];
    circleGradient?: Array<{
        offset: string;
        color: string;
    }>;
    trailPathStyle: NgStyleInterface | null;
    pathString?: string;
    icon: string;
    dir: Direction;
    trackByFn: (index: number) => string;
    get formatter(): TDSProgressFormatter;
    get _status(): TDSProgressStatusType;
    get _strokeWidth(): number;
    get isCircleStyle(): boolean;
    private cachedStatus;
    private inferredStatus;
    private destroy$;
    constructor(cdr: ChangeDetectorRef, directionality: Directionality);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    private updateIcon;
    /**
     * Calculate step render configs.
     */
    private getSteps;
    /**
     * Calculate paths when the type is circle or dashboard.
     */
    private getCirclePaths;
    private setStrokeColor;
    static ɵfac: i0.ɵɵFactoryDeclaration<TDSProgressComponent, [null, { optional: true; }]>;
    static ɵcmp: i0.ɵɵComponentDeclaration<TDSProgressComponent, "tds-progress", ["tdsProgress"], { "showInfo": "showInfo"; "width": "width"; "strokeColor": "strokeColor"; "size": "size"; "format": "format"; "successPercent": "successPercent"; "percent": "percent"; "strokeWidth": "strokeWidth"; "gapDegree": "gapDegree"; "status": "status"; "type": "type"; "gapPosition": "gapPosition"; "strokeLinecap": "strokeLinecap"; "steps": "steps"; }, {}, never, never>;
}
//# sourceMappingURL=progress.component.d.ts.map