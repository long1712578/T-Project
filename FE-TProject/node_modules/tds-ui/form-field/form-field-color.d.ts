import { Constructor, HasElementRef } from "tds-ui/shared/common";
export declare type InputColor = "default" | 'success' | 'error' | 'warning' | 'dark' | undefined;
export interface CanInputColor {
    clsByColor: string;
    clsFocusByColor: string;
    /** Theme color palette for the component. */
    color: InputColor;
    /** Default color to fall back to if no value is set. */
    defaultColor: InputColor | undefined;
}
/** @docs-private */
export declare type CanInputColorCtor = Constructor<CanInputColor>;
/** Mixin to augment a directive with a `color` property. */
export declare function mixinInputColor<T extends Constructor<HasElementRef>>(base: T, defaultColor?: InputColor): CanInputColorCtor & T;
//# sourceMappingURL=form-field-color.d.ts.map