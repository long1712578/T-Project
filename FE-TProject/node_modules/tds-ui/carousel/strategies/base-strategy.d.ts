import { Platform } from '@angular/cdk/platform';
import { ChangeDetectorRef, QueryList, Renderer2 } from '@angular/core';
import { Observable } from 'rxjs';
import { TDSSafeAny } from 'tds-ui/shared/utility';
import { TDSCarouselContentDirective } from '../carousel-content.directive';
import { FromToInterface, TDSCarouselComponentAsSource, PointerVector } from '../typings';
export declare abstract class TDSCarouselBaseStrategy<T = TDSSafeAny> {
    protected cdr: ChangeDetectorRef;
    protected renderer: Renderer2;
    protected platform: Platform;
    protected options?: T | undefined;
    protected carouselComponent: TDSCarouselComponentAsSource | null;
    protected contents: TDSCarouselContentDirective[];
    protected slickListEl: HTMLElement;
    protected slickTrackEl: HTMLElement;
    protected length: number;
    protected unitWidth: number;
    protected unitHeight: number;
    protected get maxIndex(): number;
    protected get firstEl(): HTMLElement;
    protected get lastEl(): HTMLElement;
    constructor(carouselComponent: TDSCarouselComponentAsSource, cdr: ChangeDetectorRef, renderer: Renderer2, platform: Platform, options?: T | undefined);
    /**
     * Initialize dragging sequences.
     * @param contents
     */
    withCarouselContents(contents: QueryList<TDSCarouselContentDirective> | null): void;
    /**
     * Trigger transition.
     */
    abstract switch(_f: number, _t: number): Observable<void>;
    /**
     * When user drag the carousel component.
     * @optional
     */
    dragging(_vector: PointerVector): void;
    /**
     * Destroy a scroll strategy.
     */
    dispose(): void;
    protected getFromToInBoundary(f: number, t: number): FromToInterface;
}
//# sourceMappingURL=base-strategy.d.ts.map