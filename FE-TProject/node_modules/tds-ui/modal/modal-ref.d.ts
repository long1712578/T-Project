import { OverlayRef } from '@angular/cdk/overlay';
import { Subject } from 'rxjs';
import { BaseModalContainerComponent } from './modal-container';
import { TDSModalLegacyAPI } from './modal-legacy-api';
import { ModalOptions } from './modal-types';
export declare const enum TDSModalState {
    OPEN = 0,
    CLOSING = 1,
    CLOSED = 2
}
export declare const enum TDSTriggerAction {
    CANCEL = "cancel",
    OK = "ok"
}
export declare class TDSModalRef<T = any, R = any> implements TDSModalLegacyAPI<T, R> {
    private overlayRef;
    private config;
    containerInstance: BaseModalContainerComponent;
    componentInstance: T | null;
    result?: R;
    state: TDSModalState;
    afterClose: Subject<R>;
    afterOpen: Subject<void>;
    private closeTimeout?;
    constructor(overlayRef: OverlayRef, config: ModalOptions, containerInstance: BaseModalContainerComponent);
    getContentComponent(): T;
    getElement(): HTMLElement;
    destroy(result?: R): void;
    triggerOk(): Promise<void>;
    triggerCancel(): Promise<void>;
    close(result?: R): void;
    updateConfig(config: ModalOptions): void;
    getState(): TDSModalState;
    getConfig(): ModalOptions;
    getBackdropElement(): HTMLElement | null;
    private trigger;
    private closeWhitResult;
    _finishDialogClose(): void;
}
//# sourceMappingURL=modal-ref.d.ts.map